const express = require('express') // import express
//import modal or db here
const questionsRouter = express.Router();// instantiate router

//(insert api routes here (with abbreviated url))
// questionsRouter.get('/', (req, res) => {...}
// questionsRouter.get('/:id', (req, res) => {...}
// questionsRouter.post('/', (req, res) => {...}
// questionsRouter.delete('/:id', (req, res) => {...}
// questionsRouter.put('/:id', (req, res) => {...}

module.exports = questionsRouter; //export default router;